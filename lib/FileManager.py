# обработчик переданного файла. Занимается считыванием с файла и передачей обработынных данных
class FileManager:
	ext = '.csv'

	def __init__(self, file_path):
		self.file_path = file_path

	def manage(self):
		# обработка переданного файла
		# сохранение результата и т.д.

		result = [1, 2, 3]  # для примера
		return result
